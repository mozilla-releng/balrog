name: RCE PoC
on:
  push:

jobs:
  rce:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run malicious Docker build
      run: |
        echo -e "FROM alpine\nRUN echo HACKED > /HACKED" > Dockerfile
        docker build -t test-image .
