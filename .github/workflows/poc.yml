name: RCE PoC

on:
  push:

jobs:
  rce:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Echo HACKED
        run: |
          echo "HACKED" > hacked.txt
          cat hacked.txt
