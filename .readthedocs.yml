version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.13"
  jobs:
    install:
      - uv sync --group docs --no-dev

python:
   install:
      - method: uv
        command: sync

sphinx:
  configuration: docs/source/conf.py
